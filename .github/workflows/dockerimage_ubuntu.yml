name: Docker Image CI Ubuntu

on: [push]

jobs:

  build:

    runs-on: ubuntu-bionic

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build . --file .github/DockerFile_ubuntu --tag my-image-name:$(date +%s)
